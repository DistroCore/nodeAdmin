import { ExampleModule } from './exampleModule';

export { ExampleModule } from './exampleModule';

// The plugin loader resolves the server entrypoint's default export as the NestJS module class.
export default ExampleModule;
