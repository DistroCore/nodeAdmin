import { BacklogModule } from './backlogModule';

export { BacklogModule } from './backlogModule';
export { BacklogService } from './backlogService';

// The plugin loader resolves the server entrypoint's default export as the NestJS module class.
export default BacklogModule;
