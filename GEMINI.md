# GEMINI.md（已并入 AGENTS.md）

本文件的项目规则已统一到 `AGENTS.md`，以消除双份维护造成的漂移。请阅读 `AGENTS.md`（通用 AI 代理指令）与 `CLAUDE.md`（完整命令与架构）。

> 保留本文件仅为兼容按文件名（GEMINI.md）自动加载的 agent；实质规则以 `AGENTS.md` 为准。前端 pane 实际运行为 Antigravity（agy），协作协议见 `AGENTS.md`「多 Agent 协作协议」。
